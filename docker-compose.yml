FROM node:22.12.0-alpine3.19
WORKDIR /mermaid
COPY . .
RUN corepack enable && corepack enable pnpm
RUN apk add --no-cache git
RUN pnpm install
RUN pnpm build
EXPOSE 8080
ENV PORT=8080
CMD ["pnpm", "dev"]
